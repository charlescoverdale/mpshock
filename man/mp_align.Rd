% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp_align.R
\name{mp_align}
\alias{mp_align}
\title{Align a shock series to a target data frame by date}
\usage{
mp_align(shock, target, by = "date", fill_zero = FALSE)
}
\arguments{
\item{shock}{An \code{mp_shock} object from \code{\link[=mp_shock]{mp_shock()}}.}

\item{target}{A data frame containing a date column.}

\item{by}{Character(1). The name of the date column in \code{target}.
Defaults to \code{"date"}.}

\item{fill_zero}{Logical(1). If \code{TRUE}, non-matching target rows get
\code{0} in the shock column instead of \code{NA}. Useful when the target is a
complete monthly panel and missing shocks should be treated as
zero-surprise months. Defaults to \code{FALSE}.}
}
\value{
A data frame with the same rows as \code{target} plus a \code{shock}
column (and any other numeric columns from the shock series, prefixed
with the series name).
}
\description{
Left-joins a shock series onto a target data frame by its date column.
Non-matching target rows receive \code{NA} in the shock column. Use this to
line a shock series up with a macro panel before running impulse
responses or local projections.
}
\examples{
panel <- data.frame(
  date = seq(as.Date("2010-01-01"), as.Date("2010-06-01"), by = "month"),
  gdp_growth = rnorm(6)
)
aligned <- mp_align(mp_shock("nakamura_steinsson"), panel)
head(aligned)

}
