% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp_cumulate.R
\name{mp_cumulate}
\alias{mp_cumulate}
\title{Cumulate a shock series}
\usage{
mp_cumulate(shock, window = NULL)
}
\arguments{
\item{shock}{An \code{mp_shock} object from \code{\link[=mp_shock]{mp_shock()}}.}

\item{window}{Integer, the rolling window length in observations. If
\code{NULL} (default), computes the full cumulative sum.}
}
\value{
A data frame with the same rows as \code{shock} and a new column
\code{shock_cum} (full cumulative) or \code{shock_roll} (rolling window). Other
columns are preserved.
}
\description{
Computes running sums of a shock series. With \code{window = NULL}, returns
the full cumulative sum. With a finite \code{window}, returns a rolling
window sum of the last \code{window} observations.
}
\examples{
cum <- mp_cumulate(mp_shock("nakamura_steinsson"))
head(cum)

roll <- mp_cumulate(mp_shock("nakamura_steinsson"), window = 12)
head(roll, 15)

}
